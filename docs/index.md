---
title: Project Docs
---

# Project Docs

- [API reference](/api/) — every endpoint, kept in sync automatically as the backend changes
- [Frontend](/frontend/) — components and patterns worth knowing before touching the UI

Use the search bar (top of the page, or press `/`) to find anything across all docs.

This site is generated from plain markdown in this `docs/` folder — see `docs-writer`
and `docs-plan` in `.claude/` for how it gets written and kept current.
