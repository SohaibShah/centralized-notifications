# SQL migration files live here (e.g. 001_identity.sql, 002_notifications.sql).
# Schema changes go through a migration file — never hand-edit the schema.
# First migrations land in Week 1 Task 3 (identity) and Task 5 (notifications).
